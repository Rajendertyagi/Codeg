diff --git a/src-tauri/src/acp/connection.rs b/src-tauri/src/acp/connection.rs
index 93417ed9..a050f389 100644
--- a/src-tauri/src/acp/connection.rs
+++ b/src-tauri/src/acp/connection.rs
@@ -4394,6 +4394,26 @@ async fn handle_permission_request(
         .await
         .insert(request_id.clone(), PendingPermission::Acp(responder));
 
+    // Auto-accept gate (custom plugin): when this conversation has the shield
+    // toggle enabled, answer the request with the first "allow" option instead
+    // of parking a permission card for the user. Resolves exactly like the
+    // `ConnectionCommand::RespondPermission` path below (remove -> respond ->
+    // `PermissionResolved`) so the agent sees an ordinary selected outcome.
+    let conversation_id = state.read().await.conversation_id;
+    if crate::custom_plugins::custom_auto_approve::is_auto_approved(conversation_id) {
+        if let Some(allow_option) = options
+            .iter()
+            .find(|opt| opt.kind == "allow_once" || opt.kind == "allow_always")
+        {
+            if let Some(pending) = perms.lock().await.remove(&request_id) {
+                pending.respond_selected(allow_option.option_id.clone());
+                emit_with_state(state, emitter, AcpEvent::PermissionResolved { request_id })
+                    .await;
+            }
+            return;
+        }
+    }
+
     emit_with_state(
         state,
         emitter,
diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
index 6df3f9c2..1071380a 100644
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@ -9,6 +9,10 @@
 
 pub mod acp;
 pub mod acp_transcript;
+// Custom Codeg plugins live outside the upstream crate tree (`plugins/backend/`)
+// so they survive upstream merges; see `plugins/backend/mod.rs`.
+#[path = "../../plugins/backend/mod.rs"]
+pub mod custom_plugins;
 pub use acp::{
     idle_sweep_task, idle_timeout_from_env, lifecycle_subscriber_task, SWEEP_INTERVAL_SECS,
 };
@@ -76,6 +80,7 @@ mod tauri_app {
     };
     use crate::terminal::manager::TerminalManager;
     use crate::{db, git_credential, network, paths, process, web};
+    use crate::custom_plugins;
     use tauri::Manager;
 
     static APP_QUITTING: AtomicBool = AtomicBool::new(false);
@@ -1215,6 +1220,8 @@ mod tauri_app {
                 automation_commands::automation_compute_next_run,
                 automation_commands::automation_run_now,
                 automation_commands::automation_cancel_run,
+                custom_plugins::toggle_conversation_auto_approve,
+                custom_plugins::get_conversation_auto_approve,
                 terminal_commands::terminal_spawn,
                 terminal_commands::terminal_write,
                 terminal_commands::terminal_resize,
diff --git a/src-tauri/src/web/handlers/mod.rs b/src-tauri/src/web/handlers/mod.rs
index ab2ac9a3..a279274c 100644
--- a/src-tauri/src/web/handlers/mod.rs
+++ b/src-tauri/src/web/handlers/mod.rs
@@ -1,5 +1,6 @@
 pub mod acp;
 pub mod app_update;
+pub mod auto_approve;
 pub mod automation;
 pub mod background;
 pub mod backup;
diff --git a/src-tauri/src/web/router.rs b/src-tauri/src/web/router.rs
index d72c4a18..fb634f4c 100644
--- a/src-tauri/src/web/router.rs
+++ b/src-tauri/src/web/router.rs
@@ -1146,6 +1146,15 @@ pub fn build_router(
             "/quick_messages_reorder",
             post(handlers::quick_messages::quick_messages_reorder),
         )
+        // ─── Conversation auto-approve (custom plugin) ───
+        .route(
+            "/conversation_auto_approve_get",
+            post(handlers::auto_approve::conversation_auto_approve_get),
+        )
+        .route(
+            "/conversation_auto_approve_toggle",
+            post(handlers::auto_approve::conversation_auto_approve_toggle),
+        )
         // ─── Automations ───
         .route(
             "/automation_list",
diff --git a/src/components/chat/chat-input.tsx b/src/components/chat/chat-input.tsx
index 0aea67bb..cf50997a 100644
--- a/src/components/chat/chat-input.tsx
+++ b/src/components/chat/chat-input.tsx
@@ -40,6 +40,9 @@ interface ChatInputProps {
   availableCommands?: AvailableCommandInfo[] | null
   attachmentTabId?: string | null
   draftStorageKey?: string | null
+  /** DB id of the conversation this composer feeds; forwarded to MessageInput
+   *  to key the auto-accept shield toggle. */
+  conversationId?: number | null
   isActive?: boolean
   /** Show the composer's flowing active-session border. Set only for the active
    *  tab when tiled across multiple sessions; passed through to MessageInput. */
@@ -97,6 +100,7 @@ export const ChatInput = memo(function ChatInput({
   availableCommands,
   attachmentTabId,
   draftStorageKey,
+  conversationId,
   isActive,
   showActiveFlow,
   queue,
@@ -182,6 +186,7 @@ export const ChatInput = memo(function ChatInput({
         availableCommands={availableCommands}
         attachmentTabId={attachmentTabId}
         draftStorageKey={draftStorageKey}
+        conversationId={conversationId}
         isActive={isActive}
         showActiveFlow={showActiveFlow}
         onEnqueue={onEnqueue}
diff --git a/src/components/chat/message-input.tsx b/src/components/chat/message-input.tsx
index b97a8cb9..7443914a 100644
--- a/src/components/chat/message-input.tsx
+++ b/src/components/chat/message-input.tsx
@@ -26,6 +26,8 @@ import {
   Scissors,
   Search,
   Send,
+  Shield,
+  ShieldCheck,
   Command,
   Sparkles,
   Square,
@@ -82,6 +84,8 @@ import {
   uploadLocalPathToRemote,
   isEmptyAttachmentError,
   openSettingsWindow,
+  getConversationAutoApprove,
+  toggleConversationAutoApprove,
   type SettingsSection,
   UPLOAD_MAX_BYTES,
   UPLOAD_I18N_KEY_TOO_LARGE,
@@ -228,6 +232,9 @@ interface MessageInputProps {
   promptCapabilities: PromptCapabilitiesInfo
   attachmentTabId?: string | null
   draftStorageKey?: string | null
+  /** DB id of the conversation this composer feeds. When present (an existing
+   *  conversation), the auto-accept shield toggle is shown and keyed to it. */
+  conversationId?: number | null
   isActive?: boolean
   /** Paint the flowing active-session gradient on the composer border. Set only
    *  for the active tab while tiled across multiple sessions; a lone or
@@ -515,6 +522,7 @@ export function MessageInput({
   promptCapabilities,
   attachmentTabId,
   draftStorageKey,
+  conversationId,
   isActive = false,
   showActiveFlow = false,
   onEnqueue,
@@ -568,6 +576,43 @@ export function MessageInput({
   // subset); office is a fixed static set. `tQa` supplies office labels/prompts.
   const locale = useLocale()
   const tQa = useTranslations("Folder.chat.welcomePanel.quickActions")
+  // Auto-accept shield (custom plugin): when on, the backend answers this
+  // conversation's tool-permission requests automatically instead of parking a
+  // card. Loaded when the composer mounts / the conversation changes; a null
+  // conversation (draft / welcome) hides the toggle entirely.
+  const [autoApprove, setAutoApprove] = useState(false)
+  useEffect(() => {
+    if (conversationId == null) {
+      setAutoApprove(false)
+      return
+    }
+    let cancelled = false
+    getConversationAutoApprove(conversationId)
+      .then((enabled) => {
+        if (!cancelled) setAutoApprove(enabled)
+      })
+      .catch((err) => {
+        console.error("[MessageInput] load auto-approve failed:", err)
+      })
+    return () => {
+      cancelled = true
+    }
+  }, [conversationId])
+  const handleToggleAutoApprove = useCallback(async () => {
+    if (conversationId == null) return
+    try {
+      const enabled = await toggleConversationAutoApprove(conversationId)
+      setAutoApprove(enabled)
+    } catch (err) {
+      console.error("[MessageInput] toggle auto-approve failed:", err)
+    }
+  }, [conversationId])
+  const autoApproveOnLabel = t.has("autoApproveOn")
+    ? t("autoApproveOn")
+    : "Auto-accept: on"
+  const autoApproveOffLabel = t.has("autoApproveOff")
+    ? t("autoApproveOff")
+    : "Auto-accept: off"
   const experts = useBuiltInExperts()
   const science = useBuiltInScience()
   const {
@@ -3672,7 +3717,28 @@ export function MessageInput({
                     </div>
                   )}
                 </div>
-                <div className="shrink-0">{actionButtons}</div>
+                <div className="shrink-0">
+                  {conversationId != null && (
+                    <Button
+                      onClick={handleToggleAutoApprove}
+                      variant="ghost"
+                      size="icon"
+                      className={cn("h-8 w-8", autoApprove && "text-primary")}
+                      title={autoApprove ? autoApproveOnLabel : autoApproveOffLabel}
+                      aria-label={
+                        autoApprove ? autoApproveOnLabel : autoApproveOffLabel
+                      }
+                      aria-pressed={autoApprove}
+                    >
+                      {autoApprove ? (
+                        <ShieldCheck className="size-4" />
+                      ) : (
+                        <Shield className="size-4" />
+                      )}
+                    </Button>
+                  )}
+                  {actionButtons}
+                </div>
               </div>
               {showDragActive && (
                 <div className="pointer-events-none absolute inset-1 z-20 flex items-center justify-center rounded-md border border-dashed border-primary/50 bg-background/80 text-xs text-muted-foreground">
diff --git a/src/components/conversations/conversation-detail-panel.tsx b/src/components/conversations/conversation-detail-panel.tsx
index 7e10dec0..ca8d218b 100644
--- a/src/components/conversations/conversation-detail-panel.tsx
+++ b/src/components/conversations/conversation-detail-panel.tsx
@@ -1749,6 +1749,7 @@ const ConversationTabView = memo(function ConversationTabView({
                 availableCommands={connectionCommands}
                 attachmentTabId={tabId}
                 draftStorageKey={draftStorageKey}
+                conversationId={dbConversationId}
                 isActive={isActive}
                 showActiveFlow={showActiveFlow}
                 onAddFeedback={
diff --git a/src/i18n/messages/en.json b/src/i18n/messages/en.json
index 66f518dd..827c0a59 100644
--- a/src/i18n/messages/en.json
+++ b/src/i18n/messages/en.json
@@ -2409,6 +2409,8 @@
         "noModels": "No models found",
         "cancel": "Cancel",
         "send": "Send",
+        "autoApproveOn": "Auto-accept on: tool permission requests are answered automatically",
+        "autoApproveOff": "Auto-accept off: tool permission requests require your approval",
         "forkAndSend": "Fork & Send",
         "slashCommands": "Slash commands",
         "slashSearchPlaceholder": "Search commands...",
diff --git a/src/i18n/messages/zh-CN.json b/src/i18n/messages/zh-CN.json
index 1ba2b628..1d7163a7 100644
--- a/src/i18n/messages/zh-CN.json
+++ b/src/i18n/messages/zh-CN.json
@@ -2409,6 +2409,8 @@
         "noModels": "未找到模型",
         "cancel": "取消",
         "send": "发送",
+        "autoApproveOn": "自动接受已开启：工具权限请求将自动批准",
+        "autoApproveOff": "自动接受已关闭：工具权限请求需要你批准",
         "forkAndSend": "分叉发送",
         "slashCommands": "斜杠命令",
         "slashSearchPlaceholder": "搜索命令...",
diff --git a/src/i18n/messages/zh-TW.json b/src/i18n/messages/zh-TW.json
index 63ec4f42..1095d481 100644
--- a/src/i18n/messages/zh-TW.json
+++ b/src/i18n/messages/zh-TW.json
@@ -2407,6 +2407,8 @@
         "noModels": "找不到模型",
         "cancel": "取消",
         "send": "傳送",
+        "autoApproveOn": "自動接受已開啟：工具權限請求將自動批准",
+        "autoApproveOff": "自動接受已關閉：工具權限請求需要你批准",
         "forkAndSend": "分叉發送",
         "slashCommands": "斜線命令",
         "slashSearchPlaceholder": "搜尋命令...",
diff --git a/src/lib/api.ts b/src/lib/api.ts
index b5e9ddf0..0019b4f6 100644
--- a/src/lib/api.ts
+++ b/src/lib/api.ts
@@ -2589,6 +2589,26 @@ export async function automationCancelRun(runId: number): Promise<void> {
   return getTransport().call("automation_cancel_run", { runId })
 }
 
+// Conversation auto-accept (custom plugin)
+
+/** Read whether a conversation currently has auto-accept (the shield) on. */
+export async function getConversationAutoApprove(
+  conversationId: number
+): Promise<boolean> {
+  return getTransport().call("get_conversation_auto_approve", {
+    conversationId,
+  })
+}
+
+/** Toggle auto-accept for a conversation; resolves with the new state. */
+export async function toggleConversationAutoApprove(
+  conversationId: number
+): Promise<boolean> {
+  return getTransport().call("toggle_conversation_auto_approve", {
+    conversationId,
+  })
+}
+
 // Directory browser (for web/server mode)
 
 export async function getHomeDirectory(): Promise<string> {
