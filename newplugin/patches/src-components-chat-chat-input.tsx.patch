diff --git a/src/components/chat/chat-input.tsx b/src/components/chat/chat-input.tsx
index 446f195e..81843a94 100644
--- a/src/components/chat/chat-input.tsx
+++ b/src/components/chat/chat-input.tsx
@@ -81,6 +81,9 @@ interface ChatInputProps {
    *  (new-conversation) composer, which sits in a roomy empty state; active and
    *  historical conversations keep the compact default. */
   tall?: boolean
+  /** Persisted conversation id for the per-conversation auto-accept shield.
+   *  `null` (draft / no persisted conversation) disables the shield. */
+  conversationId?: number | null
 }
 
 export const ChatInput = memo(function ChatInput({
@@ -125,6 +128,7 @@ export const ChatInput = memo(function ChatInput({
   onInjectConsumed,
   flush = false,
   tall = false,
+  conversationId = null,
 }: ChatInputProps) {
   const t = useTranslations("Folder.chat.chatInput")
   const isConnected = status === "connected"
@@ -191,6 +195,7 @@ export const ChatInput = memo(function ChatInput({
         draftStorageKey={draftStorageKey}
         isActive={isActive}
         showActiveFlow={showActiveFlow}
+        conversationId={conversationId}
         onEnqueue={onEnqueue}
         editingItemId={editingItemId}
         editingDraftText={editingDraftText}
