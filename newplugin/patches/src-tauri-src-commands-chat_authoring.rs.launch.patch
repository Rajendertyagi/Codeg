diff --git a/src-tauri/src/commands/chat_authoring.rs b/src-tauri/src/commands/chat_authoring.rs
--- a/src-tauri/src/commands/chat_authoring.rs
+++ b/src-tauri/src/commands/chat_authoring.rs
@@ -233,8 +233,10 @@
         let config = AutomationConfig {
             action: spec.action,
             prompt_blocks,
             display_text: spec.prompt.clone(),
             mode_id: None,
             config_values: BTreeMap::new(),
             label_snapshot: None,
+            existing_conversation_id: None,
+            local_folder_path: None,
         };
@@ -324,8 +326,9 @@
         let config = WorkTaskConfig {
             prompt_blocks,
             display_text: spec.prompt.clone(),
             agent_type: agent.map(|a| a.as_wire().into_owned()),
             mode_id: None,
             config_values: BTreeMap::new(),
             label_snapshot: None,
+            existing_conversation_id: None,
         };
