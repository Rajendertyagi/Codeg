diff --git a/src-tauri/src/acp/channel_messaging.rs b/src-tauri/src/acp/channel_messaging.rs
new file mode 100644
index 00000000..b38413fe
--- /dev/null
+++ b/src-tauri/src/acp/channel_messaging.rs
@@ -0,0 +1,156 @@
+//! Channel-messaging domain types — backing the `send_channel_message` MCP tool.
+//!
+//! An AI agent (interactive or automation) can explicitly send a plain-text
+//! message into the user's already-connected/enabled chat channels (Telegram,
+//! Lark, Weixin). This module holds the layer-shared pieces:
+//!
+//!   * [`SendChannelMessageRequest`] — the validated request the companion
+//!     parsed out of the tool arguments.
+//!   * [`ChannelMessageOutcome`] (+ [`ChannelSendResult`]) — the result
+//!     delivered back over the broker socket, rendered by the companion.
+//!   * [`ChannelMessageAccess`] — the listener-facing trait the production
+//!     `DbChannelMessaging` (in `crate::commands::chat_channel_messaging`)
+//!     implements. Kept as a trait so tests can stub it.
+//!   * [`ChannelMessagingConfig`] + [`ChannelMessagingRuntimeConfig`] — the
+//!     hot-swappable "is the feature on?" flag, read BOTH at MCP injection
+//!     time and again at call time (mirrors
+//!     [`crate::acp::chat_authoring::ChatAuthoringRuntimeConfig`]).
+
+use std::sync::Arc;
+
+use async_trait::async_trait;
+use serde::{Deserialize, Serialize};
+use tokio::sync::RwLock;
+
+use super::chat_authoring::truncate_chars;
+
+/// Cap on an outbound channel message. Mirrors Telegram's 4096-char limit;
+/// truncated silently (no error to the LLM) — the LLM sees whatever was
+/// actually delivered.
+pub const MAX_CHANNEL_MESSAGE_CHARS: usize = 4096;
+
+/// A validated `send_channel_message` request. The companion has already
+/// checked the required string is non-empty; ranges are re-checked by the
+/// production impl.
+#[derive(Debug, Clone, Serialize, Deserialize)]
+pub struct SendChannelMessageRequest {
+    pub token: String,
+    pub message: String,
+}
+
+/// Per-channel delivery result collected by [`DbChannelMessaging::send_message`].
+#[derive(Debug, Clone, Default, Serialize, Deserialize)]
+pub struct ChannelSendResult {
+    pub channel_id: i32,
+    pub success: bool,
+    #[serde(skip_serializing_if = "Option::is_none")]
+    pub sent_message_id: Option<String>,
+    #[serde(skip_serializing_if = "Option::is_none")]
+    pub error: Option<String>,
+}
+
+/// The outcome handed back to the tool. A refusal (`total_sent == 0`) is a
+/// soft result carrying a `note` the LLM reads and can act on — never a tool
+/// error, so a disabled feature or an empty channel set doesn't derail the
+/// turn. On success, `per_channel` reports each channel's outcome so the LLM
+/// can confirm delivery when the user has multiple enabled channels.
+#[derive(Debug, Clone, Default, Serialize, Deserialize)]
+pub struct ChannelMessageOutcome {
+    pub total_sent: usize,
+    pub total_targets: usize,
+    #[serde(skip_serializing_if = "Vec::is_empty")]
+    pub per_channel: Vec<ChannelSendResult>,
+    /// Why it was refused, or an advisory on a successful send.
+    #[serde(skip_serializing_if = "Option::is_none")]
+    pub note: Option<String>,
+}
+
+impl ChannelMessageOutcome {
+    /// A soft refusal: nothing was sent, and `note` explains why in terms the
+    /// LLM can act on (turn the setting on, enable a channel, etc.).
+    pub fn rejected(note: impl Into<String>) -> Self {
+        Self {
+            total_sent: 0,
+            total_targets: 0,
+            note: Some(note.into()),
+            ..Default::default()
+        }
+    }
+
+    /// No enabled channels exist — nothing to send to.
+    pub fn no_channels() -> Self {
+        Self {
+            total_sent: 0,
+            total_targets: 0,
+            note: Some("No enabled chat channels are connected. Ask the user to enable a channel in Settings → Channels.".to_string()),
+            ..Default::default()
+        }
+    }
+
+    /// Success path: zero or more channels were targeted, `total_sent` of them
+    /// delivered. `per_channel` carries each result.
+    pub fn sent(total_targets: usize, per_channel: Vec<ChannelSendResult>) -> Self {
+        let total_sent = per_channel.iter().filter(|r| r.success).count();
+        Self {
+            total_sent,
+            total_targets,
+            per_channel,
+            ..Default::default()
+        }
+    }
+}
+
+/// Listener-facing access for the channel-messaging tool. The production impl
+/// (`crate::commands::chat_channel_messaging::DbChannelMessaging`) re-checks
+/// the feature flag at call time, resolves the enabled-channel destination set,
+/// truncates the message, and sends via [`crate::chat_channel::ChatChannelManager`].
+#[async_trait]
+pub trait ChannelMessageAccess: Send + Sync {
+    /// Send a plain-text message to every enabled channel. Returns a soft
+    /// outcome (never throws) so the companion can render the result without
+    /// a tool error.
+    async fn send_message(
+        &self,
+        req: SendChannelMessageRequest,
+    ) -> ChannelMessageOutcome;
+}
+
+/// The single feature flag. Default OFF: unlike the read-only `get_session_info`
+/// / `ask_user_question` tools, this sends messages into the user's external
+/// channels, so the user opts in explicitly.
+#[derive(Debug, Clone, Default, PartialEq, Eq)]
+pub struct ChannelMessagingConfig {
+    pub enabled: bool,
+}
+
+/// Shared, hot-swappable handle to [`ChannelMessagingConfig`]. Cloned into
+/// `DelegationInjection` (read at injection, to build `--features`) and into
+/// `AppState` (updated on save).
+///
+/// It is ALSO read again at call time by the production access impl. The
+/// read-only tools get away with an injection-time-only check — their tools
+/// stay listed for the life of an already-running session after the user flips
+/// the setting off. For a tool that sends messages externally, "off" has to
+/// mean off right now, so the write path re-reads this handle.
+#[derive(Clone, Default)]
+pub struct ChannelMessagingRuntimeConfig {
+    inner: Arc<RwLock<ChannelMessagingConfig>>,
+}
+
+impl ChannelMessagingRuntimeConfig {
+    pub fn new() -> Self {
+        Self::default()
+    }
+
+    pub async fn snapshot(&self) -> ChannelMessagingConfig {
+        self.inner.read().await.clone()
+    }
+
+    pub async fn set(&self, cfg: ChannelMessagingConfig) {
+        *self.inner.write().await = cfg;
+    }
+
+    pub async fn enabled(&self) -> bool {
+        self.inner.read().await.enabled
+    }
+}
diff --git a/src-tauri/src/acp/delegation/companion.rs b/src-tauri/src/acp/delegation/companion.rs
index dad5fc59..1e7c9897 100644
--- a/src-tauri/src/acp/delegation/companion.rs
+++ b/src-tauri/src/acp/delegation/companion.rs
@@ -44,14 +44,16 @@ use tokio::sync::{oneshot, Mutex};
 use crate::acp::chat_authoring::{
     NewAutomationSpec, NewWorkTaskSpec, MAX_PROMPT_CHARS, MAX_TITLE_CHARS,
 };
+use crate::acp::channel_messaging::ChannelMessageOutcome;
 use crate::acp::delegation::transport::{
     client_ask_round_trip, client_cancel, client_cancel_task_round_trip, client_commit_feedback,
     client_create_automation_round_trip, client_create_work_task_round_trip,
-    client_feedback_round_trip, client_round_trip, client_session_round_trip,
-    client_status_round_trip, client_task_complete_round_trip, client_task_progress_round_trip,
-    BrokerAskRequest, BrokerCancelRequest, BrokerCancelTaskRequest, BrokerCommitFeedbackRequest,
-    BrokerCreateAutomationRequest, BrokerCreateWorkTaskRequest, BrokerFeedbackRequest,
-    BrokerRequest, BrokerResponse, BrokerSessionRequest, BrokerStatusRequest,
+    client_feedback_round_trip, client_round_trip, client_send_channel_message_round_trip,
+    client_session_round_trip, client_status_round_trip, client_task_complete_round_trip,
+    client_task_progress_round_trip, BrokerAskRequest, BrokerCancelRequest,
+    BrokerCancelTaskRequest, BrokerCommitFeedbackRequest, BrokerCreateAutomationRequest,
+    BrokerCreateWorkTaskRequest, BrokerFeedbackRequest, BrokerRequest, BrokerResponse,
+    BrokerSendChannelMessageRequest, BrokerSessionRequest, BrokerStatusRequest,
     BrokerTaskCompleteRequest, BrokerTaskProgressRequest,
 };
 use crate::acp::question::parse_questions;
@@ -152,6 +154,11 @@ pub struct CompanionFeatures {
     pub automations: bool,
     /// `create_work_task` — queue a card on the work-task board from chat.
     pub taskboard: bool,
+    /// `send_channel_message` — let the AI push a message into the user's
+    /// enabled chat channels (Telegram, Lark, Weixin). Gated separately so
+    /// the authoring tools and the channel-send tool can be toggled
+    /// independently.
+    pub channels: bool,
 }
 
 impl CompanionFeatures {
@@ -171,6 +178,7 @@ impl CompanionFeatures {
                 tasks: false,
                 automations: false,
                 taskboard: false,
+                channels: false,
             };
         };
         let mut f = Self {
@@ -181,6 +189,7 @@ impl CompanionFeatures {
             tasks: false,
             automations: false,
             taskboard: false,
+            channels: false,
         };
         for tok in s.split(',').map(str::trim).filter(|t| !t.is_empty()) {
             match tok {
@@ -191,6 +200,7 @@ impl CompanionFeatures {
                 "tasks" => f.tasks = true,
                 "automations" => f.automations = true,
                 "taskboard" => f.taskboard = true,
+                "channels" => f.channels = true,
                 _ => {}
             }
         }
@@ -206,6 +216,7 @@ impl CompanionFeatures {
             "task_progress" | "task_complete" => self.tasks,
             "create_automation" => self.automations,
             "create_work_task" => self.taskboard,
+            "send_channel_message" => self.channels,
             "delegate_to_agent" | "get_delegation_status" | "cancel_delegation" => self.delegation,
             _ => false,
         }
@@ -726,6 +737,22 @@ async fn build_tools_call_spawn(
                 Box::pin(async move { client_create_work_task_round_trip(&socket, &req).await });
             register_and_spawn(inflight, id, None, round_trip, render_authoring_result).await
         }
+        "send_channel_message" => {
+            let msg = match serde_json::from_value::<SendChannelMessageArgs>(arguments) {
+                Ok(a) => a,
+                Err(e) => return LineAction::Respond(err(id, -32602, format!("invalid args: {e}"))),
+            };
+            if msg.message.is_empty() {
+                return LineAction::Respond(err(id, -32602, "message must not be empty"));
+            }
+            let req = BrokerSendChannelMessageRequest {
+                token: ctx.token.clone(),
+                message: msg.message,
+            };
+            let round_trip =
+                Box::pin(async move { client_send_channel_message_round_trip(&socket, &req).await });
+            register_and_spawn(inflight, id, None, round_trip, render_channel_message_result).await
+        }
         other => LineAction::Respond(err(id, -32602, format!("unknown tool: {other}"))),
     }
 }
@@ -1408,6 +1435,59 @@ pub fn render_authoring_result(outcome: &Value) -> Value {
     })
 }
 
+/// Arguments parsed from a `send_channel_message` `tools/call` request.
+#[derive(Debug, Clone, Deserialize)]
+struct SendChannelMessageArgs {
+    message: String,
+}
+
+/// Render a [`ChannelMessageOutcome`] back into an MCP `tools/call` result.
+/// Mirrors [`render_authoring_result`] but formats per-channel delivery
+/// summaries so the LLM knows exactly which channels received the message.
+pub fn render_channel_message_result(outcome: &Value) -> Value {
+    let total_sent = outcome.get("total_sent").and_then(|v| v.as_u64()).unwrap_or(0) as usize;
+    let total_targets = outcome.get("total_targets").and_then(|v| v.as_u64()).unwrap_or(0) as usize;
+    let note = outcome.get("note").and_then(|v| v.as_str());
+    let per_channel = outcome.get("per_channel").and_then(|v| v.as_array());
+
+    let text = if total_sent > 0 {
+        let mut parts = vec![format!("Sent to {total_sent} of {total_targets} channel(s)")];
+        if let Some(arr) = per_channel {
+            for item in arr {
+                let ch_id = item.get("channel_id").and_then(|v| v.as_i64()).unwrap_or(0);
+                let success = item.get("success").and_then(|v| v.as_bool()).unwrap_or(false);
+                let msg_id = item
+                    .get("sent_message_id")
+                    .and_then(|v| v.as_str())
+                    .unwrap_or("");
+                if success {
+                    if !msg_id.is_empty() {
+                        parts.push(format!("#{ch_id} → {msg_id}"));
+                    } else {
+                        parts.push(format!("#{ch_id} ✓"));
+                    }
+                } else {
+                    let err = item
+                        .get("error")
+                        .and_then(|v| v.as_str())
+                        .unwrap_or("failed");
+                    parts.push(format!("#{ch_id} ✗ {err}"));
+                }
+            }
+        }
+        parts.join("\n")
+    } else {
+        note
+            .unwrap_or("Message was not sent; no reason was reported.")
+            .to_string()
+    };
+    json!({
+        "content": [{ "type": "text", "text": text }],
+        "isError": false,
+        "structuredContent": outcome.clone(),
+    })
+}
+
 /// Build the human-readable summary block for a found session: a metadata header
 /// plus, when present, a "Recent messages" section.
 fn render_session_summary_text(o: &Value) -> String {
diff --git a/src-tauri/src/acp/delegation/listener.rs b/src-tauri/src/acp/delegation/listener.rs
index 4b51ad60..d99deb31 100644
--- a/src-tauri/src/acp/delegation/listener.rs
+++ b/src-tauri/src/acp/delegation/listener.rs
@@ -20,12 +20,13 @@ use crate::acp::delegation::transport::{
     read_frame, write_frame, BrokerAskRequest, BrokerCancelRequest, BrokerCancelTaskRequest,
     BrokerCommitFeedbackRequest, BrokerFeedbackRequest, BrokerMessage, BrokerRequest,
     BrokerCreateAutomationRequest, BrokerCreateWorkTaskRequest, BrokerResponse,
-    BrokerSessionRequest, BrokerStatusRequest, BrokerTaskCompleteRequest,
-    BrokerTaskProgressRequest,
+    BrokerSendChannelMessageRequest, BrokerSessionRequest, BrokerStatusRequest,
+    BrokerTaskCompleteRequest, BrokerTaskProgressRequest,
 };
 use crate::acp::delegation::types::{DelegationRequest, DelegationTaskReport, TaskStatus};
 use crate::acp::feedback::{PendingFeedback, SessionFeedbackAccess};
 use crate::acp::question::{QuestionOutcome, SessionQuestionAccess};
+use crate::acp::channel_messaging::{ChannelMessageAccess, ChannelMessageOutcome};
 use crate::acp::chat_authoring::{AuthoringContext, AuthoringOutcome, ChatAuthoringAccess};
 use crate::acp::session_info::{SessionInfo, SessionInfoAccess};
 use crate::acp::work_task_tools::{TaskReportAck, WorkTaskToolAccess};
@@ -108,6 +109,10 @@ pub struct DelegationListener {
     /// feature flags at call time, so flipping the setting off stops writes
     /// from sessions that were launched while it was on.
     pub authoring: Arc<dyn ChatAuthoringAccess>,
+    /// Sends messages into the user's enabled chat channels (`send_channel_message`).
+    /// The impl re-checks the feature flag at call time, so flipping the setting
+    /// off stops sends from sessions that were launched while it was on.
+    pub channel_messaging: Arc<dyn ChannelMessageAccess>,
 }
 
 impl DelegationListener {
@@ -121,6 +126,7 @@ impl DelegationListener {
         session_info: Arc<dyn SessionInfoAccess>,
         tasks: Arc<dyn WorkTaskToolAccess>,
         authoring: Arc<dyn ChatAuthoringAccess>,
+        channel_messaging: Arc<dyn ChannelMessageAccess>,
     ) -> Arc<Self> {
         Arc::new(Self {
             broker,
@@ -131,6 +137,7 @@ impl DelegationListener {
             session_info,
             tasks,
             authoring,
+            channel_messaging,
         })
     }
 
@@ -344,6 +351,14 @@ impl DelegationListener {
             BrokerMessage::CreateWorkTask(req) => {
                 authoring_response(self.process_create_work_task(req).await)?
             }
+            BrokerMessage::SendChannelMessage(req) => {
+                let outcome = self.process_send_channel_message(req).await;
+                BrokerResponse {
+                    outcome: serde_json::to_value(outcome).map_err(|e| {
+                        std::io::Error::new(std::io::ErrorKind::Other, e.to_string())
+                    })?,
+                }
+            }
             BrokerMessage::Cancel(cancel) => {
                 self.process_cancel(cancel).await;
                 // Empty ack — the companion only uses this to detect the
@@ -581,6 +596,24 @@ impl DelegationListener {
         self.authoring.create_work_task(ctx, req.spec).await
     }
 
+    /// Validate the token and hand the channel-message request to the messaging
+    /// impl, which re-checks the feature flag before sending.
+    async fn process_send_channel_message(
+        &self,
+        req: BrokerSendChannelMessageRequest,
+    ) -> ChannelMessageOutcome {
+        let Some(_ctx) = self.authoring_context(&req.token).await else {
+            return ChannelMessageOutcome::rejected("invalid token");
+        };
+        use crate::acp::channel_messaging::SendChannelMessageRequest;
+        self.channel_messaging
+            .send_message(SendChannelMessageRequest {
+                token: req.token,
+                message: req.message,
+            })
+            .await
+    }
+
     async fn process(&self, req: BrokerRequest) -> DelegationTaskReport {
         // 1. Token + parent_connection_id consistency check. Treat both as
         //    "canceled" since the LLM can't usefully react to either —
diff --git a/src-tauri/src/app_state.rs b/src-tauri/src/app_state.rs
index 7777e9c0..11bff04e 100644
--- a/src-tauri/src/app_state.rs
+++ b/src-tauri/src/app_state.rs
@@ -65,6 +65,12 @@ pub struct AppState {
     /// updated by the chat-authoring settings command on save. Populated at
     /// startup by `apply_persisted_chat_authoring_config`.
     pub chat_authoring_config: crate::acp::chat_authoring::ChatAuthoringRuntimeConfig,
+    /// Hot-swappable channel-messaging (`send_channel_message`) enable flag.
+    /// Shared with the `DelegationInjection` so MCP injection reads it,
+    /// re-read by the messaging write path at call time, and updated by the
+    /// channel-messaging settings command on save. Populated at startup by
+    /// `apply_persisted_channel_messaging_config`.
+    pub chat_channel_messaging_config: crate::acp::channel_messaging::ChannelMessagingRuntimeConfig,
     /// Serializes mutually-exclusive system operations — in-place
     /// self-update, restart, rollback — so a second click can't race a
     /// download/swap already in flight. Handlers `try_lock` and reject when
@@ -116,6 +122,7 @@ pub fn build_delegation_stack(
     crate::acp::question::QuestionRuntimeConfig,
     crate::acp::session_info::SessionInfoRuntimeConfig,
     crate::acp::chat_authoring::ChatAuthoringRuntimeConfig,
+    crate::acp::channel_messaging::ChannelMessagingRuntimeConfig,
 ) {
     use crate::acp::connection::DelegationInjection;
     use crate::acp::delegation::broker::{
@@ -167,6 +174,7 @@ pub fn build_delegation_stack(
     let ask = crate::acp::question::QuestionRuntimeConfig::new();
     let sessions = crate::acp::session_info::SessionInfoRuntimeConfig::new();
     let authoring = crate::acp::chat_authoring::ChatAuthoringRuntimeConfig::new();
+    let channel_messaging = crate::acp::channel_messaging::ChannelMessagingRuntimeConfig::new();
 
     // Install the injection on the manager so spawn_agent picks it up
     // without an extra parameter at every call site.
@@ -179,6 +187,7 @@ pub fn build_delegation_stack(
         ask: ask.clone(),
         sessions: sessions.clone(),
         authoring: authoring.clone(),
+        channel_messaging: channel_messaging.clone(),
         // Same backing manager as the listener's question lookup; used only by
         // the run_connection teardown guard to reclaim a parked ask.
         questions: Arc::new(crate::acp::manager::ConnectionManagerQuestionLookup {
@@ -191,7 +200,7 @@ pub fn build_delegation_stack(
         }) as Arc<dyn crate::acp::plan_approval::SessionPlanApprovalAccess>,
     });
 
-    (broker, tokens, socket_path, feedback, ask, sessions, authoring)
+    (broker, tokens, socket_path, feedback, ask, sessions, authoring, channel_messaging)
 }
 
 impl AppState {
diff --git a/src-tauri/src/bin/codeg_server.rs b/src-tauri/src/bin/codeg_server.rs
index 715aacab..15f2f32e 100644
--- a/src-tauri/src/bin/codeg_server.rs
+++ b/src-tauri/src/bin/codeg_server.rs
@@ -265,6 +265,7 @@ async fn async_main() -> ExitCode {
         question_config,
         session_info_config,
         chat_authoring_config,
+        channel_messaging_config,
     ) = codeg_lib::app_state::build_delegation_stack(
         &connection_manager,
         db.conn.clone(),
@@ -291,6 +292,7 @@ async fn async_main() -> ExitCode {
         question_config: question_config.clone(),
         session_info_config: session_info_config.clone(),
         chat_authoring_config: chat_authoring_config.clone(),
+        channel_messaging_config: channel_messaging_config.clone(),
         system_op_lock: codeg_lib::app_state::default_system_op_lock(),
         update_state: codeg_lib::app_state::default_update_state(),
     });
@@ -334,6 +336,13 @@ async fn async_main() -> ExitCode {
         &chat_authoring_config,
     )
     .await;
+    // Same for the channel-messaging flag, so the first companion launch knows
+    // whether to advertise `send_channel_message`.
+    codeg_lib::commands::chat_channel_messaging::apply_persisted_channel_messaging_config(
+        &state.db.conn,
+        &channel_messaging_config,
+    )
+    .await;
 
     // Spawn the delegation listener so companion processes can round-trip
     // through the broker. Path is PID-scoped, so the listener owns it for
@@ -364,6 +373,14 @@ async fn async_main() -> ExitCode {
                 state.emitter.clone(),
                 chat_authoring_config.clone(),
             )),
+            Arc::new(codeg_lib::commands::chat_channel_messaging::DbChannelMessaging::new(
+                Arc::new(codeg_lib::db::AppDatabase {
+                    conn: state.db.conn.clone(),
+                }),
+                state.emitter.clone(),
+                channel_messaging_config.clone(),
+                state.chat_channel_manager.clone_ref(),
+            )),
         );
         let socket = delegation_socket_path.clone();
         tokio::spawn(async move {
diff --git a/src-tauri/src/commands/chat_channel_messaging.rs b/src-tauri/src/commands/chat_channel_messaging.rs
new file mode 100644
index 00000000..f28d9e9a
--- /dev/null
+++ b/src-tauri/src/commands/chat_channel_messaging.rs
@@ -0,0 +1,252 @@
+//! `send_channel_message` backing logic + settings persistence.
+//!
+//! Two surfaces live here, mirroring `crate::commands::chat_authoring`:
+//!
+//!   * [`DbChannelMessaging`] — the production [`ChannelMessageAccess`] impl the
+//!     delegation listener calls when a chat agent asks codeg to send a message
+//!     into the user's connected channels. It re-checks the feature flag,
+//!     truncates the message, iterates enabled channels, sends via
+//!     [`crate::chat_channel::ChatChannelManager`], and logs each attempt.
+//!   * The `chat_channel_messaging.enabled` setting knob (**default false**) —
+//!     read at MCP injection time via [`ChannelMessagingRuntimeConfig`] to build
+//!     `--features`, and AGAIN at call time by [`DbChannelMessaging`] so
+//!     turning the switch off stops sends from sessions that were launched
+//!     while it was on.
+
+use std::sync::Arc;
+
+use async_trait::async_trait;
+use sea_orm::DatabaseConnection;
+use serde::{Deserialize, Serialize};
+
+use crate::acp::channel_messaging::{
+    ChannelMessageAccess, ChannelMessageOutcome, ChannelMessagingConfig, ChannelMessagingRuntimeConfig,
+    ChannelSendResult, MAX_CHANNEL_MESSAGE_CHARS,
+};
+use crate::acp::chat_authoring::truncate_chars;
+use crate::app_error::AppCommandError;
+use crate::chat_channel::manager::ChatChannelManager;
+use crate::chat_channel::types::RichMessage;
+use crate::commands::chat_authoring::load_flag;
+use crate::db::service::{
+    app_metadata_service, chat_channel_message_log_service, chat_channel_service,
+};
+use crate::db::AppDatabase;
+use crate::web::event_bridge::{emit_event, EventEmitter, CHAT_AUTHORING_SETTINGS_CHANGED_EVENT};
+
+const KEY_CHANNEL_MESSAGING_ENABLED: &str = "chat_channel_messaging.enabled";
+
+/// Production [`ChannelMessageAccess`]. Holds the DB, the event emitter (unused
+/// here — no state-change broadcast needed), the runtime config it re-checks
+/// before every send, and the chat-channel manager that does the actual dispatch.
+pub struct DbChannelMessaging {
+    pub db: Arc<AppDatabase>,
+    pub emitter: EventEmitter,
+    pub config: ChannelMessagingRuntimeConfig,
+    pub chat_channel_manager: ChatChannelManager,
+}
+
+impl DbChannelMessaging {
+    pub fn new(
+        db: Arc<AppDatabase>,
+        emitter: EventEmitter,
+        config: ChannelMessagingRuntimeConfig,
+        chat_channel_manager: ChatChannelManager,
+    ) -> Self {
+        Self {
+            db,
+            emitter,
+            config,
+            chat_channel_manager,
+        }
+    }
+}
+
+#[async_trait]
+impl ChannelMessageAccess for DbChannelMessaging {
+    async fn send_message(
+        &self,
+        req: crate::acp::channel_messaging::SendChannelMessageRequest,
+    ) -> ChannelMessageOutcome {
+        // Re-check at call time, not just at injection: a session launched while
+        // the switch was on keeps the tool listed after the user turns it off,
+        // and "off" has to stop the send.
+        if !self.config.enabled().await {
+            return ChannelMessageOutcome::rejected(
+                "Sending messages through chat channels is turned off in codeg's settings \
+                 (Settings → General → AI Channel Messaging). Ask the user to enable it.",
+            );
+        }
+
+        let conn = &self.db.conn;
+
+        // Resolve the native enabled-channel destination set — the same set every
+        // automatic outbound producer (event_subscriber, daily-report scheduler) uses.
+        let enabled_channels = match chat_channel_service::list_enabled(conn).await {
+            Ok(channels) => channels,
+            Err(e) => {
+                tracing::error!("[channel_messaging] failed to list enabled channels: {e}");
+                return ChannelMessageOutcome::rejected(format!("Could not resolve destination channels: {e}"));
+            }
+        };
+
+        if enabled_channels.is_empty() {
+            return ChannelMessageOutcome::no_channels();
+        }
+
+        // Truncate silently to Telegram's 4096-char limit (char-counted, not
+        // byte-counted, so multi-byte text is never split mid-codepoint).
+        let message = truncate_chars(&req.message, MAX_CHANNEL_MESSAGE_CHARS);
+        let mut per_channel = Vec::with_capacity(enabled_channels.len());
+
+        for ch in &enabled_channels {
+            let rich_msg = RichMessage::info(message.clone());
+            match self.chat_channel_manager.send_to_channel(ch.id, &rich_msg).await {
+                Ok(sent_id) => {
+                    let id_str = sent_id.0;
+                    let preview = &message[..message.len().min(200)];
+                    let _ = chat_channel_message_log_service::create_log(
+                        conn,
+                        ch.id,
+                        "outbound",
+                        "agent_send",
+                        preview,
+                        "sent",
+                        None,
+                    )
+                    .await;
+                    per_channel.push(ChannelSendResult {
+                        channel_id: ch.id,
+                        success: true,
+                        sent_message_id: Some(id_str),
+                        error: None,
+                    });
+                }
+                Err(e) => {
+                    tracing::warn!(
+                        "[channel_messaging] send_to_channel(ch={}) failed: {e}",
+                        ch.id
+                    );
+                    let preview = &message[..message.len().min(200)];
+                    let _ = chat_channel_message_log_service::create_log(
+                        conn,
+                        ch.id,
+                        "outbound",
+                        "agent_send",
+                        preview,
+                        "failed",
+                        Some(e.to_string()),
+                    )
+                    .await;
+                    per_channel.push(ChannelSendResult {
+                        channel_id: ch.id,
+                        success: false,
+                        sent_message_id: None,
+                        error: Some(e.to_string()),
+                    });
+                }
+            }
+        }
+
+        ChannelMessageOutcome::sent(enabled_channels.len(), per_channel)
+    }
+}
+
+// Settings persistence — `chat_channel_messaging.enabled` (default OFF).
+// Mirrors `crate::commands::chat_authoring`.
+// ===========================================================================
+
+#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq, Eq)]
+pub struct ChannelMessagingSettings {
+    pub enabled: bool,
+}
+
+impl ChannelMessagingSettings {
+    fn into_runtime_config(self) -> ChannelMessagingConfig {
+        ChannelMessagingConfig {
+            enabled: self.enabled,
+        }
+    }
+}
+
+pub async fn load_channel_messaging_settings(
+    conn: &DatabaseConnection,
+) -> ChannelMessagingSettings {
+    ChannelMessagingSettings {
+        enabled: load_flag(conn, KEY_CHANNEL_MESSAGING_ENABLED).await,
+    }
+}
+
+pub async fn apply_persisted_channel_messaging_config(
+    conn: &DatabaseConnection,
+    config: &ChannelMessagingRuntimeConfig,
+) {
+    let settings = load_channel_messaging_settings(conn).await;
+    config.set(settings.into_runtime_config()).await;
+}
+
+pub async fn set_chat_channel_messaging_settings_core(
+    conn: &DatabaseConnection,
+    config: &ChannelMessagingRuntimeConfig,
+    emitter: &EventEmitter,
+    desired: ChannelMessagingSettings,
+) -> Result<ChannelMessagingSettings, AppCommandError> {
+    app_metadata_service::upsert_value(
+        conn,
+        KEY_CHANNEL_MESSAGING_ENABLED,
+        &desired.enabled.to_string(),
+    )
+    .await
+    .map_err(AppCommandError::from)?;
+    config.set(desired.clone().into_runtime_config()).await;
+    emit_event(emitter, CHAT_AUTHORING_SETTINGS_CHANGED_EVENT, &desired);
+    Ok(desired)
+}
+
+// -------- Tauri commands -----------------------------------------------------
+
+#[cfg_attr(feature = "tauri-runtime", tauri::command)]
+pub async fn get_chat_channel_messaging_settings(
+    #[cfg(feature = "tauri-runtime")] db: tauri::State<'_, crate::db::AppDatabase>,
+) -> Result<ChannelMessagingSettings, AppCommandError> {
+    #[cfg(feature = "tauri-runtime")]
+    {
+        Ok(load_channel_messaging_settings(&db.conn).await)
+    }
+    #[cfg(not(feature = "tauri-runtime"))]
+    {
+        Err(AppCommandError::configuration_invalid("tauri-only command"))
+    }
+}
+
+#[cfg_attr(feature = "tauri-runtime", tauri::command)]
+pub async fn set_chat_channel_messaging_settings(
+    #[cfg(feature = "tauri-runtime")] app: tauri::AppHandle,
+    #[cfg(feature = "tauri-runtime")] db: tauri::State<'_, crate::db::AppDatabase>,
+    #[cfg(feature = "tauri-runtime")] config: tauri::State<'_, ChannelMessagingRuntimeConfig>,
+    settings: ChannelMessagingSettings,
+) -> Result<ChannelMessagingSettings, AppCommandError> {
+    #[cfg(feature = "tauri-runtime")]
+    {
+        let emitter = EventEmitter::Tauri(app);
+        set_chat_channel_messaging_settings_core(&db.conn, &config, &emitter, settings).await
+    }
+    #[cfg(not(feature = "tauri-runtime"))]
+    {
+        let _ = settings;
+        Err(AppCommandError::configuration_invalid("tauri-only command"))
+    }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn settings_into_runtime_config_flips_enabled() {
+        let off = ChannelMessagingSettings { enabled: false };
+        assert!(!off.into_runtime_config().enabled);
+        let on = ChannelMessagingSettings { enabled: true };
+        assert!(on.into_runtime_config().enabled);
+    }
+}
diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
index 2a75c083..3b0f265c 100644
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@ -527,6 +527,7 @@ mod tauri_app {
                         question_config,
                         session_info_config,
                         chat_authoring_config,
+                        channel_messaging_config,
                     ) = crate::app_state::build_delegation_stack(
                         &cm_state,
                         db_conn.clone(),
@@ -538,6 +539,7 @@ mod tauri_app {
                     app.manage(question_config.clone());
                     app.manage(session_info_config.clone());
                     app.manage(chat_authoring_config.clone());
+                    app.manage(channel_messaging_config.clone());
                     app.manage(crate::commands::delegation::DelegationSocketPath(
                         socket_path.clone(),
                     ));
@@ -550,6 +552,7 @@ mod tauri_app {
                     let question_for_init = question_config.clone();
                     let session_info_for_init = session_info_config.clone();
                     let chat_authoring_for_init = chat_authoring_config.clone();
+                    let channel_messaging_for_init = channel_messaging_config.clone();
                     tauri::async_runtime::block_on(async move {
                         delegation_commands::apply_persisted_config(
                             &db_for_init,
@@ -576,6 +579,11 @@ mod tauri_app {
                             &chat_authoring_for_init,
                         )
                         .await;
+                        crate::commands::chat_channel_messaging::apply_persisted_channel_messaging_config(
+                            &db_for_init,
+                            &channel_messaging_for_init,
+                        )
+                        .await;
                     });
 
                     let listener_broker = broker.clone();
@@ -616,6 +624,18 @@ mod tauri_app {
                                 chat_authoring_config.clone(),
                             ),
                         ),
+                        std::sync::Arc::new(
+                            crate::commands::chat_channel_messaging::DbChannelMessaging::new(
+                                std::sync::Arc::new(db::AppDatabase {
+                                    conn: db_conn.clone(),
+                                }),
+                                crate::web::event_bridge::EventEmitter::Tauri(
+                                    app.handle().clone(),
+                                ),
+                                channel_messaging_config.clone(),
+                                cm_state.chat_channel_manager.clone_ref(),
+                            ),
+                        ),
                     );
                     tauri::async_runtime::spawn(async move {
                         if let Err(e) = listener.run(socket_path).await {
diff --git a/src-tauri/src/web/handlers/chat_channel_messaging.rs b/src-tauri/src/web/handlers/chat_channel_messaging.rs
new file mode 100644
index 00000000..f92f9192
--- /dev/null
+++ b/src-tauri/src/web/handlers/chat_channel_messaging.rs
@@ -0,0 +1,43 @@
+//! HTTP handlers for channel-messaging settings — the web-mode mirror of the
+//! Tauri commands in `commands::chat_channel_messaging`.
+//!
+//! Both endpoints share the same core helpers (`load_channel_messaging_settings`,
+//! `set_chat_channel_messaging_settings_core`) so the persist + runtime-config
+//! re-apply behavior stays identical across transports.
+
+use std::sync::Arc;
+
+use axum::{extract::Extension, Json};
+use serde::Deserialize;
+
+use crate::app_error::AppCommandError;
+use crate::app_state::AppState;
+use crate::commands::chat_channel_messaging::{
+    load_channel_messaging_settings, set_chat_channel_messaging_settings_core,
+    ChannelMessagingSettings,
+};
+
+pub async fn get_chat_channel_messaging_settings(
+    Extension(state): Extension<Arc<AppState>>,
+) -> Result<Json<ChannelMessagingSettings>, AppCommandError> {
+    Ok(Json(load_channel_messaging_settings(&state.db.conn).await))
+}
+
+#[derive(Deserialize)]
+pub struct SetChannelMessagingSettingsParams {
+    pub settings: ChannelMessagingSettings,
+}
+
+pub async fn set_chat_channel_messaging_settings(
+    Extension(state): Extension<Arc<AppState>>,
+    Json(params): Json<SetChannelMessagingSettingsParams>,
+) -> Result<Json<ChannelMessagingSettings>, AppCommandError> {
+    let saved = set_chat_channel_messaging_settings_core(
+        &state.db.conn,
+        &state.chat_channel_messaging_config,
+        &state.emitter,
+        params.settings,
+    )
+    .await?;
+    Ok(Json(saved))
+}
diff --git a/src-tauri/src/web/mod.rs b/src-tauri/src/web/mod.rs
index 659c1b2f..48a62d48 100644
--- a/src-tauri/src/web/mod.rs
+++ b/src-tauri/src/web/mod.rs
@@ -836,6 +836,12 @@ pub(crate) async fn do_start_web_server_tauri(
             .state::<crate::acp::chat_authoring::ChatAuthoringRuntimeConfig>()
             .inner()
             .clone(),
+        // Same channel-messaging config handle the desktop MCP injection reads,
+        // so HTTP-side saves target the same flag.
+        chat_channel_messaging_config: app
+            .state::<crate::acp::channel_messaging::ChannelMessagingRuntimeConfig>()
+            .inner()
+            .clone(),
         system_op_lock: crate::app_state::default_system_op_lock(),
         // Reuse the same handle the desktop `app_update` commands write to so
         // HTTP and webview readers see the identical update snapshot.
diff --git a/src/components/settings/chat-channel-messaging-settings.tsx b/src/components/settings/chat-channel-messaging-settings.tsx
new file mode 100644
index 00000000..3a8de4e0
--- /dev/null
+++ b/src/components/settings/chat-channel-messaging-settings.tsx
@@ -0,0 +1,125 @@
+"use client"
+
+/**
+ * AI Channel Messaging settings panel — a single kill switch persisted as
+ * `chat_channel_messaging.enabled` on the Rust side.
+ *
+ * When on, `codeg-mcp` exposes `send_channel_message` so an agent can push a
+ * text message into the user's already-connected/enabled chat channels
+ * (Telegram, Lark, Weixin). Ship OFF: the tool writes external state, so the
+ * user opts in explicitly. Mounted under `/settings/general` with the other
+ * MCP-tool toggles.
+ */
+
+import { useCallback, useEffect, useState } from "react"
+import { useTranslations } from "next-intl"
+import { Loader2, Radio } from "lucide-react"
+import { toast } from "sonner"
+
+import { Button } from "@/components/ui/button"
+import { Switch } from "@/components/ui/switch"
+import {
+  type ChannelMessagingSettings,
+  getChannelMessagingSettings,
+  setChannelMessagingSettings,
+} from "@/lib/api"
+import { toErrorMessage } from "@/lib/app-error"
+
+export function ChannelMessagingSettingsSection() {
+  const t = useTranslations("ChannelMessagingSettings")
+  const [loading, setLoading] = useState(true)
+  const [saving, setSaving] = useState(false)
+  const [enabled, setEnabled] = useState(false)
+  const [loadError, setLoadError] = useState<string | null>(null)
+
+  useEffect(() => {
+    let cancelled = false
+    void getChannelMessagingSettings()
+      .then((s) => {
+        if (cancelled) return
+        setEnabled(s.enabled)
+        setLoadError(null)
+      })
+      .catch((err: unknown) => {
+        if (cancelled) return
+        setLoadError(toErrorMessage(err))
+      })
+      .finally(() => {
+        if (cancelled) return
+        setLoading(false)
+      })
+    return () => {
+      cancelled = true
+    }
+  }, [])
+
+  const save = useCallback(async () => {
+    const payload: ChannelMessagingSettings = { enabled }
+    setSaving(true)
+    try {
+      const applied = await setChannelMessagingSettings(payload)
+      setEnabled(applied.enabled)
+      toast.success(t("saved"))
+    } catch (err: unknown) {
+      toast.error(t("saveFailed"), { description: toErrorMessage(err) })
+    } finally {
+      setSaving(false)
+    }
+  }, [enabled, t])
+
+  return (
+    <section className="rounded-xl border bg-card p-4 space-y-4">
+      <div className="flex items-center gap-2">
+        <Radio className="h-4 w-4 text-muted-foreground" aria-hidden />
+        <h2 className="text-sm font-semibold">{t("title")}</h2>
+      </div>
+      <p className="text-xs text-muted-foreground leading-5">
+        {t("description")}
+      </p>
+
+      {loadError && (
+        <p className="rounded-md border border-destructive/30 bg-destructive/5 px-3 py-2 text-xs text-destructive">
+          {t("loadFailed", { detail: loadError })}
+        </p>
+      )}
+
+      <div className="flex items-center justify-between gap-3">
+        <div className="space-y-1 min-w-0">
+          <label
+            htmlFor="channel-messaging-enabled"
+            className="flex items-center gap-1.5 text-sm font-medium"
+          >
+            <Radio
+              className="h-3.5 w-3.5 text-muted-foreground"
+              aria-hidden
+            />
+            {t("enable")}
+          </label>
+          <p className="text-xs text-muted-foreground">
+            {t("enableHint")}
+          </p>
+        </div>
+        <Switch
+          id="channel-messaging-enabled"
+          checked={enabled}
+          onCheckedChange={setEnabled}
+          disabled={loading}
+          className="shrink-0"
+        />
+      </div>
+
+      <div className="flex justify-end pt-2">
+        <Button onClick={save} disabled={loading || saving} size="sm">
+          {saving ? (
+            <>
+              <Loader2 className="h-3.5 w-3.5 animate-spin" />
+              {t("saving")}
+            </>
+          ) : (
+            t("save")
+          )}
+        </Button>
+      </div>
+    </section>
+  )
+}
