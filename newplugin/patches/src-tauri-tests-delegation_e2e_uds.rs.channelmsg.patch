diff --git a/src-tauri/tests/delegation_e2e_uds.rs b/src-tauri/tests/delegation_e2e_uds.rs
index bd6c91b8..53003fcd 100644
--- a/src-tauri/tests/delegation_e2e_uds.rs
+++ b/src-tauri/tests/delegation_e2e_uds.rs
@@ -119,6 +119,21 @@ impl codeg_lib::acp::chat_authoring::ChatAuthoringAccess for NoAuthoring {
     }
 }
 
+/// Channel-messaging stub: the e2e delegation tests never exercise the
+/// `send_channel_message` arm.
+struct NoChannelMessaging;
+#[async_trait]
+impl codeg_lib::acp::channel_messaging::ChannelMessageAccess for NoChannelMessaging {
+    async fn send_message(
+        &self,
+        _req: codeg_lib::acp::channel_messaging::SendChannelMessageRequest,
+    ) -> codeg_lib::acp::channel_messaging::ChannelMessageOutcome {
+        codeg_lib::acp::channel_messaging::ChannelMessageOutcome::rejected(
+            "stub channel messaging",
+        )
+    }
+}
+
 /// Controllable question access for the ask round-trip test: `register_question`
 /// parks a sender keyed by a freshly-minted id; the test pops it via
 /// `take_pending` and resolves it, exactly as a user answering the card would.
@@ -208,6 +223,7 @@ async fn end_to_end_uds_happy_path() {
         Arc::new(NoSessionInfo) as Arc<dyn codeg_lib::acp::session_info::SessionInfoAccess>,
         Arc::new(NoTaskTools) as Arc<dyn codeg_lib::acp::work_task_tools::WorkTaskToolAccess>,
         Arc::new(NoAuthoring) as Arc<dyn codeg_lib::acp::chat_authoring::ChatAuthoringAccess>,
+        Arc::new(NoChannelMessaging) as Arc<dyn codeg_lib::acp::channel_messaging::ChannelMessageAccess>,
     );
 
     // PID-scoped socket inside the OS temp dir — no clashes across test bins.
@@ -325,6 +341,7 @@ async fn end_to_end_uds_batch_status() {
         Arc::new(NoSessionInfo) as Arc<dyn codeg_lib::acp::session_info::SessionInfoAccess>,
         Arc::new(NoTaskTools) as Arc<dyn codeg_lib::acp::work_task_tools::WorkTaskToolAccess>,
         Arc::new(NoAuthoring) as Arc<dyn codeg_lib::acp::chat_authoring::ChatAuthoringAccess>,
+        Arc::new(NoChannelMessaging) as Arc<dyn codeg_lib::acp::channel_messaging::ChannelMessageAccess>,
     );
 
     let dir = tempfile::tempdir().unwrap();
@@ -413,6 +430,7 @@ async fn end_to_end_uds_invalid_token_rejected() {
         Arc::new(NoSessionInfo) as Arc<dyn codeg_lib::acp::session_info::SessionInfoAccess>,
         Arc::new(NoTaskTools) as Arc<dyn codeg_lib::acp::work_task_tools::WorkTaskToolAccess>,
         Arc::new(NoAuthoring) as Arc<dyn codeg_lib::acp::chat_authoring::ChatAuthoringAccess>,
+        Arc::new(NoChannelMessaging) as Arc<dyn codeg_lib::acp::channel_messaging::ChannelMessageAccess>,
     );
 
     let dir = tempfile::tempdir().unwrap();
@@ -480,6 +498,7 @@ async fn end_to_end_uds_ask_question_round_trip() {
         Arc::new(NoSessionInfo) as Arc<dyn codeg_lib::acp::session_info::SessionInfoAccess>,
         Arc::new(NoTaskTools) as Arc<dyn codeg_lib::acp::work_task_tools::WorkTaskToolAccess>,
         Arc::new(NoAuthoring) as Arc<dyn codeg_lib::acp::chat_authoring::ChatAuthoringAccess>,
+        Arc::new(NoChannelMessaging) as Arc<dyn codeg_lib::acp::channel_messaging::ChannelMessageAccess>,
     );
 
     let dir = tempfile::tempdir().unwrap();
@@ -621,6 +640,7 @@ async fn end_to_end_uds_ask_revoked_after_register_declines() {
         Arc::new(NoSessionInfo) as Arc<dyn codeg_lib::acp::session_info::SessionInfoAccess>,
         Arc::new(NoTaskTools) as Arc<dyn codeg_lib::acp::work_task_tools::WorkTaskToolAccess>,
         Arc::new(NoAuthoring) as Arc<dyn codeg_lib::acp::chat_authoring::ChatAuthoringAccess>,
+        Arc::new(NoChannelMessaging) as Arc<dyn codeg_lib::acp::channel_messaging::ChannelMessageAccess>,
     );
 
     let dir = tempfile::tempdir().unwrap();
