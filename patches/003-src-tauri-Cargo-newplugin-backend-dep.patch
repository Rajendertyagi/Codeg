diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index 2db7beb0..a026b8a8 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -1045,6 +1045,7 @@ dependencies = [
  "libc",
  "mac-notification-sys",
  "minisign-verify",
+ "newplugin-backend",
  "notify",
  "portable-pty",
  "prost",
@@ -3761,6 +3762,10 @@ dependencies = [
  "minimal-lexical",
 ]
 
+[[package]]
+name = "newplugin-backend"
+version = "0.1.0"
+
 [[package]]
 name = "notify"
 version = "6.1.1"
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index d482bf36..0a6c3c5a 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -102,6 +102,9 @@ notify = "6"
 base64 = "0.22"
 agent-client-protocol-schema = { version = "0.11", features = ["unstable_session_usage", "unstable_session_fork", "unstable_session_resume", "unstable_elicitation"] }
 kill_tree = { version = "0.2", features = ["tokio"] }
+# Layer-2 plugin backend (`newplugin/`): decision logic for new functionality.
+# Plain-data in, plain-decision out ΓÇö never depends back on this crate.
+newplugin-backend = { path = "../newplugin/backend" }
 which = "7"
 keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"], optional = true }
 axum = { version = "0.8", features = ["ws", "multipart"] }
